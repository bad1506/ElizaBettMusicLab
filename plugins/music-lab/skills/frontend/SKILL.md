---
name: music-lab-frontend
description: Build and review the React/Vite frontend without breaking existing product flows.
---

# Music Lab Frontend

- Preserve the existing React 18 + TypeScript + Vite architecture.
- Inspect current `ai-engineer-ui/src/App.tsx` and related components before editing.
- Never replace the whole app when a targeted change is sufficient.
- Preserve RU/EN support, Telegram Mini App links, tool contexts, audio upload validation, and API fallback behavior.
- Keep the approved visual direction: bright editorial minimalism, premium typography, rounded UI, no human-face imagery.
- Use accessible buttons, labels, keyboard behavior, responsive layouts, and mobile-first checks.
- Run `npm run build` before declaring frontend work complete.
